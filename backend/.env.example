# Demo credentials (local only). Do not commit real secrets.
DEMO_EMAIL=demo@example.com
# Use a real password here for backend-only runs. The literal placeholder below is rejected by seed_demo_data.
# Docker Compose sets DEMO_PASSWORD to demo123 by default (see docker-compose.yml) unless you override it in the shell.
DEMO_PASSWORD=demo123
